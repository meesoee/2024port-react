import React from "react";

const Header = () => {
    return (
        <header>
            <a href="/">LOGO</a>
            <nav>Navigation</nav>
            <a href="/">global</a>
            <a href="/">TOP</a>
        </header>
    );
};

export default Header;